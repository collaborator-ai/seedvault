{
  "name": "seedvault",
  "private": true,
  "workspaces": ["server", "client", "sdk"],
  "scripts": {
    "dev:server": "bun run --filter @seedvault/server dev",
    "dev:cli": "bun run --filter @seedvault/client dev",
    "check": "bun run --filter '*' check",
    "test": "bun test test/"
  }
}
