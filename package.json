{
  "name": "seedvault",
  "private": true,
  "workspaces": ["server", "cli", "electron"],
  "scripts": {
    "dev:server": "bun run --filter @seedvault/server dev",
    "dev:cli": "bun run --filter @seedvault/cli dev",
    "check": "bun run --filter '*' check",
    "test": "bun test test/"
  }
}
